# Скапіюйце ў local.properties (файл не ў git).
# Калі totus.publicApiKey пусты — Gradle бярэ publicApiKey з ../totus-app-version.properties (адзін крыніца з вэб-панэллю).
# Перавызначэнне для лакальнай зборкі: totus.publicApiKey=...
totus.publicApiKey=
