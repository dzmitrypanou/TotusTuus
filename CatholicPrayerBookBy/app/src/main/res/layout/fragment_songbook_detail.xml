<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_dark"
    tools:context=".ui.songbook.SongbookDetailFragment">

    <WebView
        android:id="@+id/web_songbook_body"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:overScrollMode="ifContentScrolls"
        android:scrollbars="none"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/scroll_songbook_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"
        android:visibility="gone">

        <by.dzmitrypanou.catholicapp.ui.songbook.ZoomableImageView
            android:id="@+id/image_songbook"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/songbook_image_content_description"
            android:scaleType="fitCenter" />
    </FrameLayout>
</FrameLayout>
